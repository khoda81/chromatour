[package]
name = "chromatour-core"
version = "0.1.0"
edition = "2024"
description = "Perceptual color-tour objective and browser-side baseline solver for Chromatour"
publish = false

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
wasm-bindgen = "0.2"
